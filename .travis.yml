language: Swift
osx_image: xcode9.3
xcode_project: Utils/Utils.xcodeproj
xcode_scheme: Utils
before_install:
  - gem install xcpretty -N
  - brew update
  - brew install swiftlint || true
script:
  - set -o pipefail
