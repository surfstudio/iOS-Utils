//
//  StyleExtension.swift
//  Utils
//
//  Created by Никита Гагаринов on 30.10.2020.
//  Copyright © 2020 Surf. All rights reserved.
//

// MARK: - Label

public extension UILabel {
    func apply(style: UIStyle<UILabel>) {
        style.apply(for: self)
    }
}

// MARK: - TextField

public extension UITextField {
    func apply(style: UIStyle<UITextField>) {
        style.apply(for: self)
    }
}

// MARK: - Button

public extension UIButton {
    func apply(style: UIStyle<UIButton>) {
        style.apply(for: self)
    }
}

// MARK: - CommonButton

public extension CommonButton {
    func apply(style: UIStyle<CommonButton>) {
        style.apply(for: self)
    }
}

// MARK: - UIImageView

public extension UIImageView {
    func apply(style: UIStyle<UIImageView>) {
        style.apply(for: self)
    }
}

// MARK: - UIView

public extension UIView {
    func apply(style: UIStyle<UIView>) {
        style.apply(for: self)
    }
}

// MARK: - UIProgressView

public extension UIProgressView {
    func apply(style: UIStyle<UIProgressView>) {
        style.apply(for: self)
    }
}

// MARK: - UICollectionViewCell

public extension UICollectionViewCell {
    func apply(style: UIStyle<UICollectionViewCell>) {
        style.apply(for: self)
    }
}

// MARK: - UINavigationBar

public extension UINavigationBar {
    func apply(style: UIStyle<UINavigationBar>) {
        style.apply(for: self)
    }
}

// MARK: - UISwitch

public extension UISwitch {
    func apply(style: UIStyle<UISwitch>) {
        style.apply(for: self)
    }
}

// MARK: - UISegmentedControl

public extension UISegmentedControl {
    func apply(style: UIStyle<UISegmentedControl>) {
        style.apply(for: self)
    }
}

// MARK: - UISegmentedControl

public extension UITableView {
    func apply(style: UIStyle<UITableView>) {
        style.apply(for: self)
    }
}

// MARK: - UITextView

public extension UITextView {
    func apply(style: UIStyle<UITextView>) {
        style.apply(for: self)
    }
}

// MARK: - UISlider

public extension UISlider {
    func apply(style: UIStyle<UISlider>) {
        style.apply(for: self)
    }
}

// MARK: - SkeletonView

extension SkeletonView {
    func apply(style: UIStyle<SkeletonView>) {
        style.apply(for: self)
    }
}

// MARK: - UITabBar

extension UITabBar {
    func apply(style: UIStyle<UITabBar>) {
        style.apply(for: self)
    }
}

// MARK: - UIPageControl

extension UIPageControl {
    func apply(style: UIStyle<UIPageControl>) {
        style.apply(for: self)
    }
}
